# Stacked external-method figures

`mosta_external_methods_stacked` presents the matched MOSTA sections as rows of
ground-truth or method-specific spatial-domain assignments. It is assembled by
`scripts/external_benchmark/plot_external_method_clusters.py` from aligned
method outputs.
