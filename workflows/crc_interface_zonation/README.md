# CRC interface zonation

This bundle contains the stacked CRC tumor-periphery distance-zonation marker summary.

Result:

- `crc_interface_zonation_stacked.png`

Code:

- `../../scripts/crc_visium_hd/make_crc_interface_zonation_stacked.py`
